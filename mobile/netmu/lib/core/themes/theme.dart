import 'dart:ui';

class ColorTheme {
  ColorTheme._();

  // ── Surfaces ───────────────────────────
  static const Color background = Color(0xFFF7F5F0);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF0EDE8);
  static const Color border = Color(0xFFE7E4DF);
  static const Color borderStrong = Color(0xFFD6D3CE);

  // ── Text ───────────────────────────────
  static const Color textPrimary = Color(0xFF1C1917);
  static const Color textSecondary = Color(0xFF78716C);
  static const Color textOnAccent = Color(0xFFFFFFFF);

  // ── Buttons ────────────────────────────
  static const Color buttonPrimary = Color(0xFF1C4A3B);
  static const Color buttonPrimaryHover = Color(0xFF153829);
  static const Color buttonSecondary = Color(0xFFF0EDE8);
  static const Color buttonSecondaryText = Color(0xFF1C1917);
  static const Color buttonDanger = Color(0xFFB91C1C);
  static const Color buttonDangerHover = Color(0xFF991B1B);

  // ── Accent ─────────────────────────────
  static const Color accent = Color(0xFFC2410C);
  static const Color accentSoft = Color(0xFFFEF3EE);

  // ── Semantic ───────────────────────────
  static const Color success = Color(0xFF166534);
  static const Color successSoft = Color(0xFFDCFCE7);
  static const Color warning = Color(0xFF92400E);
  static const Color warningSoft = Color(0xFFFEF3C7);
  static const Color info = Color(0xFF1E40AF);
  static const Color infoSoft = Color(0xFFDBEAFE);
}
